# NOVA XMD • Pair Site (Render Ready)

## Routes
- `/` Accueil
- `/pair` Page Pairing (boutons + copie)
- `/dashboard` Sessions actives

## API
- `/api/pair?number=+226xxxxxxxx` -> { ok, code }
- `/api/health`
- `/api/status`

## Render
Build: `npm install`
Start: `npm start`

> IMPORTANT: le dossier `session/` contient les sessions multi-numéros.
